{
  "name": "filmroom",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "deploy": "bun run build && mkdir -p dist/.hunk && cp .hunk/config.json dist/.hunk/config.json && hunk push dist",
    "preview": "vite preview --host 127.0.0.1",
    "test": "bun test"
  },
  "dependencies": {
    "@cornerstonejs/codec-openjpeg": "1.3.6",
    "dicom-parser": "1.8.21",
    "fflate": "0.8.3"
  },
  "devDependencies": {
    "vite": "8.2.1"
  }
}
